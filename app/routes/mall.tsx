import NotFoundYet from "~/tests/views/futureScreen";

export default function MallRoute() {
  return (
    <div style={{ height: "calc(100vh - 32px)", minHeight: 0 }}>
      <NotFoundYet />
    </div>
  );
}

